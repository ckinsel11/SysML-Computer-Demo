1. Walk through configuration management example over time (i.e. Old computer upgrades to newer configuration)
2. Define Process for how I assembled my computer and not generic process
3. Figure out how to show interface between cpu and cpu cooler 
4. CPU Cooler Mount interface. Need to show that one comes installed by default but then swapped out to other via hardware in box.
5. Define rules for using abstract
6. Define rules for naming convention (camelcase vs lower vs upper vs normal)
7. Male vs Female RAM
8. Make an example of a "custom connector". Since computers are all standards based I'm combining the connector with the communication in a singular port. However this isn't how to do it on a custom box, as you don't need to store that combination then use it. You can just make it defined with the box.
9. Review Edge Connectors. Do they need contact arrangements?
10. comparison on multiplicity of values to a single value if any are equal (in Computer constraint for mb form factor)
11. Review derived values/items (weighted score calculated via raw score and weight factor)